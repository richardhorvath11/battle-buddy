{
  "description": "The intended shipped-plugin bundle (FR-010). Until slice 4+ creates the real plugin manifest, this fixture stands in for it; the packaging test's input then switches from fixture to real manifest with no logic change (research R7).",
  "bundle": [
    "commands/**",
    "agents/**",
    "skills/**",
    "hooks/**",
    "bin/**",
    "manifest/**",
    "templates/**",
    "README.md",
    "LICENSE"
  ]
}
